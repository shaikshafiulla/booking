-- This script runs when the database is first created
-- You can add initial data or schema modifications here

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Add any initial setup queries here
-- For example, creating indexes, setting up permissions, etc.

-- Sample comment
-- INSERT INTO "Users" (name, email, password, role, "createdAt", "updatedAt") 
-- VALUES ('Admin', 'admin@system.com', 'hashed_password', 'admin', NOW(), NOW());
